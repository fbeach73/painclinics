# Action Required: Blog SEO Enhancements

Manual steps that must be completed by a human. These cannot be automated.

## Before Implementation

- [ ] **Verify OpenRouter API key is set** - Required for AI alt text generation. Check `OPENROUTER_API_KEY` is configured in Vercel environment variables.

## During Implementation

No manual steps required during implementation.

## After Implementation

- [ ] **Test with ZimmWriter** - Send a test post with an image to verify all three features work in production
- [ ] **Validate FAQ schema** - Use Google Rich Results Test (https://search.google.com/test/rich-results) to verify FAQ schema is valid

---

> **Note:** These tasks are also listed in context within `implementation-plan.md`
